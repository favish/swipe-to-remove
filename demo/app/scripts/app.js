'use strict';

angular.module('demoApp', ['ngRoute', 'ngAnimate', 'itemSwipe'])
  .config(function ($routeProvider) {
    $routeProvider
      .when('/', {
        templateUrl: 'views/main.html',
        controller: 'MainCtrl'
      })
      .otherwise({
        redirectTo: '/'
      });
  });
